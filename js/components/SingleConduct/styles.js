import {StyleSheet} from 'react-native';
const styles = StyleSheet.create({
  conductTitle: {
    paddingBottom: 20,
    color: '#9963ea',
    fontSize: 18,
  },
  conductDescription: {
    fontSize: 17,
    lineHeight: 25,
    paddingBottom: 20,
  },
});

export default styles;
